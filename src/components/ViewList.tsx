import React from "react";
import { NavLink } from "react-router-dom";

export default function ViewList(menu: string) {
  function createViewList(item: string): string {
    return "";
  }
  return (
    <>
      <NavLink to={{}}></NavLink>
    </>
  );
}
